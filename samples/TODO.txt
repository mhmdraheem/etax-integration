I want to create another webpage that accepts uploading CSV file, then processes it, then download WinRAR ZIP archive having Json files.
The uploaded CSV is a file having invoices and credit notes for my noon sales. I want you to extract some information from
this file, and use it, alongside the template requests I will provide for you, to create Json file(s) to be submitted.
The Json file(s) should be compressed in a WinRAR ZIP archive and be available for download.

* What to extract from CSV file:
Column name: Document Type (Invoice/Creditnote)
Column name: Partner SKU
Column name: Price Including VAT (Document Currency)
Column name: Document Date

* Use the receipt.json, and return-receipt.json files as template files to create the target Json files.
Each Json file should have the exact format of receipt.json but with multiple submissions, if applicable (check batch-receipt1.json, and batch-receipt2.json)

Now, for data mapping in receipt.json, and return-receipt.json:
dateTimeIssued -> Column name: Document Date. Format 2026-05-24T00:34:00Z, time 00:00:00
receiptNumber -> this either 'Invoice Nr' or 'Credit Note Nr'
uuid -> to be calculated
previousUUID -> to be calculated
referenceUUID -> to be calculated
currency -> Fixed config, use as is
receiptType -> 'S' for sales, 'R' for credit note
typeVersion -> Fixed config, use as is
rin -> Fixed config, use as is
companyTradeName -> Fixed config, use as is
branchCode -> Fixed config, use as is
country -> Fixed config, use as is
governate -> Fixed config, use as is
regionCity -> Fixed config, use as is
street -> Fixed config, use as is
buildingNumber -> Fixed config, use as is
deviceSerialNumber -> Fixed config, use as is
activityCode -> Fixed config, use as is
type -> Fixed config, use as is
itemData[].internalCode -> check product mapping
itemData[].description -> check product mapping
itemData[].itemType ->  -> Fixed config, use as is
itemData[].itemCode -> check product mapping
itemData[].unitType -> check product mapping
itemData[].quantity -> based on invoice data in excel sheet
itemData[].unitPrice -> As per doc: Mandatory, cost per quantity of the product
itemData[].netSale -> As per doc: Mandatory, Total amount for the receipt line after applying discount.
itemData[].totalSale -> As per doc: Mandatory, Total amount for the receipt line considering quantity and unit price
itemData[].total -> As per doc: Mandatory, Total amount for the receipt line after adding all pricing items, taxes, removing discounts
itemData[].taxableItems[].taxType  -> Fixed config, use as is
itemData[].taxableItems[].amount -> To be calculated
itemData[].taxableItems[].subType  -> Fixed config, use as is
itemData[].taxableItems[].rate  -> Fixed config, use as is
totalSales -> As per doc: Mandatory, sum of all sales total elements of receipt lines
netAmount -> As per doc: Mandatory, Sum of all receipt lines netTotal
totalAmount -> As per doc: Mandatory, totalAmount = sum of all receipt line total – total extraDiscountAmount
paymentMethod -> Fixed config, use as is

Product mapping:
if Partner SKU = CHAR-200-BOX-VBUNDLE
internalCode -> CHAR-200-BOX-VBUNDLE
description -> Fast-Lighting Charcoal for Incense | 200 Tablets
itemCode -> EG-776878123-CHAR200BOX-VBUNDLE
unitType -> PK

if Partner SKU = CHAR-100-BOX
internalCode -> CHAR-100-BOX
description -> Fast-Lighting Charcoal for Incense | 100 Tablets
itemCode -> EG-776878123-CHAR100BOX
unitType -> BOX

if Partner SKU = CHAR-50-BUNDLE
internalCode -> CHAR-50-BUNDLE
description -> Fast-Lighting Charcoal for Incense | 50 Tablets
itemCode -> EG-776878123-CHAR50BUNDLE
unitType -> PK

if Partner SKU = CHAR-30-BUNDLE
internalCode -> CHAR-30-BUNDLE
description -> Fast-Lighting Charcoal for Incense | 30 Tablets
itemCode -> EG-776878123-CHAR30BUNDLE
unitType -> PK

if Partner SKU = CHAR-10-BUNDLE
internalCode -> CHAR-10-BUNDLE
description -> Fast-Lighting Charcoal for Incense | 10 Tablets
itemCode -> EG-776878123-CHAR10BUNDLE
unitType -> EA

* When dealing with invoices, some clients buy multiple items, in that case, you will find multiple
duplicated rows with same 'Invoice Nr', but with different 'Invoice Line Nr' either for one product 
or multiple products

* When dealing with returns, you should also take care of how many lines were returned because sometimes
only a subset of items are returned ('Credit Note Nr', and 'Credit Note Line Nr')

* For now, itemData[].taxableItems[] will not be included. So, create a config flag includeVAT=false.
Include the VAT calculation logic but make applying it conditional. Note that the price of unit you
get from Noon excel sheet is inclusive of vat

* Rules for creating the ZIP archive:
Zip file should contain max 100 submission files in JSON format, and max 500 receipts in each submission
Max sizes allowed: Zip 25 Mb , JSON 2560 Kb each

* I want to keep track of sent orders/returns so save them in a mySQL DB. with each row saving order details, including it's 
request, and uuid, and all of them linked to a single batch number. Batch data should be also saved

* UUID field to be calculated as same algorithm in current website index.html
* previousUUID is empty for first request ever, otherwise it's the one used in prev. request
* referenceUUID is only used in case of returns, and refers to uuid of original order which you can retrieve from DB (using Invoice Nr)

UI design
---------
I want to place the upload, and process buttons on top right corner
And below them, I want to list processed orders in a scrollable table shape, with each row showing order details
like number, lines count, price, with expandable arrow on click that shows the lines details.
Order will be colored light green, return will be colored light red. With total count showing under the table and order/return values total sum.
If an order or return has multiple lines.
On the right of this table, I want an empty space to display this order's Json object to be sent.
Below the table, a button to view the Json file(s) and abutton to sownload the WinRAR archive
Incase any field is missing, add an alert icon in the row with reason visible on hover